{
  "name": "CostFinders",
  "install": "npm ci",
  "ports": [{ "name": "web", "port": 3000 }],
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Next.js dev server on http://localhost:3000. Runs without Supabase credentials; pages show local-demo setup guidance until NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY are provided."
    }
  ]
}
