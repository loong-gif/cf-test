---
phase: 08-admin-core
plan: 01
type: execute
---

<objective>
Create admin authentication context and dashboard shell with icon-only sidebar navigation.

Purpose: Establish admin portal foundation following the business dashboard pattern - auth context, protected layout, and navigation structure.
Output: AdminAuthContext, AdminDashboardSidebar, and auth-gated dashboard layout ready for admin pages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-business-dashboard/07-01-SUMMARY.md

**Pattern references:**
@src/lib/context/businessAuthContext.tsx
@src/components/layout/businessDashboardSidebar.tsx
@src/app/business/dashboard/layout.tsx

**Established patterns:**
- Icon-only sidebar with tooltips (64px width)
- Auth context with state, signIn, signOut, isLoading
- Protected layout with redirect on unauthenticated
- Mobile bottom nav with first 5 items
- Glassmorphic styling with backdrop-blur-xl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdminAuthContext and admin mock data</name>
  <files>src/types/admin.ts, src/lib/mock-data/admins.ts, src/lib/mock-data/index.ts, src/lib/context/adminAuthContext.tsx</files>
  <action>
    1. Create Admin type in src/types/admin.ts with fields: id, email, firstName, lastName, role ('super_admin' | 'moderator' | 'support'), permissions array, createdAt, updatedAt, lastLoginAt
    2. Export from src/types/index.ts
    3. Create src/lib/mock-data/admins.ts with 2-3 mock admin users (one super_admin, one moderator)
    4. Export admins from mock-data/index.ts
    5. Create AdminAuthContext following businessAuthContext.tsx pattern:
       - State: admin, isAuthenticated, isLoading
       - Actions: signIn(email, password), signOut()
       - localStorage key: 'costfinders_admin_id'
       - Provider wraps children, exports useAdminAuth hook

    Follow businessAuthContext exactly for structure. Admin sign-in validates against admins array by email (mock auth, no password check like business auth).
  </action>
  <verify>TypeScript compiles with no errors. AdminAuthContext exports correctly.</verify>
  <done>Admin type defined, mock admins exist, AdminAuthContext provides auth state and methods</done>
</task>

<task type="auto">
  <name>Task 2: Create AdminDashboardSidebar and dashboard layout</name>
  <files>src/components/layout/adminDashboardSidebar.tsx, src/app/admin/dashboard/layout.tsx, src/app/admin/dashboard/page.tsx, src/app/admin/page.tsx</files>
  <action>
    1. Create AdminDashboardSidebar following businessDashboardSidebar.tsx pattern:
       - Nav items: Home (House), Deals (Tag), Users (Users), Businesses (Buildings), Content (FolderSimple), Settings (Gear)
       - Icon-only desktop sidebar (w-16, left-0, top-20)
       - Mobile bottom nav with first 5 items
       - Tooltip on hover using existing Tooltip component
       - Sign out button at bottom with SignOut icon
       - Use useAdminAuth for state and signOut

    2. Create src/app/admin/dashboard/layout.tsx:
       - Wrap with AdminAuthProvider
       - Protected route: redirect to /admin if not authenticated
       - Include AdminDashboardSidebar
       - Main content with md:pl-16 for sidebar clearance
       - Loading state while checking auth

    3. Create src/app/admin/dashboard/page.tsx:
       - Simple placeholder: "Admin Dashboard" heading
       - Will be replaced in 08-02 with full content

    4. Create src/app/admin/page.tsx as admin entry/login page:
       - Simple sign-in form (email only for mock auth)
       - Card with glassmorphic styling
       - Redirect to /admin/dashboard on successful sign-in
       - Use existing Input, Button components
  </action>
  <verify>npm run build passes. Navigate to /admin shows login. After login, /admin/dashboard shows sidebar and placeholder content.</verify>
  <done>Admin dashboard shell complete with auth-gated access, sidebar navigation, and login page</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] /admin page shows login form
- [ ] Login with mock admin email redirects to /admin/dashboard
- [ ] AdminDashboardSidebar displays with all nav items
- [ ] Tooltips appear on hover
- [ ] Mobile bottom nav shows on small screens
- [ ] Sign out returns to /admin
- [ ] Direct access to /admin/dashboard without auth redirects to /admin
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Admin auth flow works end-to-end
- Dashboard shell matches business dashboard quality
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-core/08-01-SUMMARY.md`
</output>
