---
phase: 09-admin-platform
plan: 01
type: execute
---

<objective>
Create platform reporting dashboard with aggregated metrics across all businesses, deals, and users.

Purpose: Give admins visibility into platform-wide performance, growth trends, and key health metrics.
Output: Reporting page at /admin/dashboard/reports with platform metrics, charts, and activity summary.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/08-admin-core/08-04-SUMMARY.md

# Existing patterns:
@src/app/admin/dashboard/page.tsx
@src/components/features/analytics/analyticsDashboard.tsx
@src/components/layout/adminDashboardSidebar.tsx

**Established patterns:**
- MetricCard component with icon, value, label, trend indicators
- Platform overview metrics grid (4 cards)
- Deal performance table with status badges
- Insights/trends section with icon-led list items
- Admin sidebar with icon-only navigation + tooltips
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create platform reporting page with metrics dashboard</name>
  <files>src/app/admin/dashboard/reports/page.tsx</files>
  <action>
Create platform reporting dashboard at /admin/dashboard/reports with:

1. **Header section**: "Platform Reports" title with "Platform-wide performance and analytics" subtitle

2. **Platform Health Metrics** (4-card grid, reuse MetricCard pattern):
   - Total Platform Revenue (mock: $847,250, +18% trend)
   - Active Businesses (count from businesses mock data, filter active status)
   - Monthly Claims (mock: 1,234, +23% trend)
   - User Growth (count consumers, +156 this month trend)

3. **Time Period Selector** (above metrics):
   - Tabs: "Last 7 Days" | "Last 30 Days" | "Last 90 Days" | "All Time"
   - Default to "Last 30 Days"
   - Mock data doesn't change, just visual state

4. **Growth Trends Section** (2-column grid on lg, stacked on mobile):
   Left column - "Business Activity":
   - Table with columns: Metric | This Period | Change
   - Rows: New Businesses, New Claims, Deal Views, Messages Sent
   - Use mock data with +/- percentages

   Right column - "Top Performing Categories":
   - List of categories with claim counts
   - Use categories from mock data, add mock claim counts
   - Show bar chart visualization (CSS-only, div width percentages)

5. **Recent Platform Activity** (full width):
   - List showing last 10 activities (mock data inline)
   - Each item: icon + description + timestamp
   - Activity types: new_business, new_claim, deal_approved, tier_upgrade, new_user
   - Use existing Badge component for activity type

Use 'use client' directive. Import from existing mock data files for counts.
Follow existing admin page patterns from dashboard home page.
  </action>
  <verify>npm run build passes, page renders at /admin/dashboard/reports</verify>
  <done>Platform reporting page displays metrics, trends, category breakdown, and activity feed</done>
</task>

<task type="auto">
  <name>Task 2: Add Reports link to admin sidebar navigation</name>
  <files>src/components/layout/adminDashboardSidebar.tsx</files>
  <action>
Add "Reports" navigation item to admin sidebar:

1. Import ChartBar icon from @phosphor-icons/react
2. Add to navItems array after "Content" item:
   { href: '/admin/dashboard/reports', label: 'Reports', icon: ChartBar }

This adds Reports between Content and Settings in the navigation order.
Mobile nav will automatically include it (first 5 items shown).
  </action>
  <verify>Admin sidebar shows Reports link, clicking navigates to /admin/dashboard/reports</verify>
  <done>Reports link visible in sidebar, active state works when on reports page</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] /admin/dashboard/reports page loads correctly
- [ ] Metrics display with correct counts from mock data
- [ ] Time period tabs switch visual state
- [ ] Growth trends section renders with mock data
- [ ] Activity feed shows recent platform events
- [ ] Reports link appears in admin sidebar
- [ ] Reports link shows active state when on page
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Reports page follows established admin dashboard patterns
- Navigation integrates cleanly with existing sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/09-admin-platform/09-01-SUMMARY.md`
</output>
