---
phase: 14-module-polish
plan: 01
type: execute
---

<objective>
Migrate auth and form components to semantic color tokens.

Purpose: Replace hardcoded Tailwind colors (red-400, green-400, etc.) with semantic tokens (error, success, etc.) in authentication and form components.
Output: 5 components using semantic tokens consistently for error/success states.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Token migration reference (from 11-03)
@.planning/phases/11-design-system-audit/11-03-SUMMARY.md

# Token migration pattern:
| Original | Semantic |
|----------|----------|
| bg-green-500/10 | bg-success/10 |
| text-green-400 | text-success-text |
| bg-red-500/10 | bg-error/10 |
| text-red-400 | text-error-text |
| bg-amber-500/10 | bg-warning/10 |
| text-amber-400 | text-warning-text |
| bg-blue-500/10 | bg-info/10 |
| text-blue-400 | text-info-text |

**Tech stack available:** Next.js 16, React, TypeScript, Tailwind CSS v4
**Semantic tokens available:** success, warning, error, info (+ -text variants)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate auth form components to semantic tokens</name>
  <files>src/components/features/signInForm.tsx, src/components/features/signUpForm.tsx, src/components/features/phoneVerification.tsx, src/components/features/emailVerification.tsx</files>
  <action>
Update each auth form component to use semantic color tokens:

**signInForm.tsx:**
- Line 115: `text-green-400 bg-green-500/10` → `text-success-text bg-success/10`
- Line 121: `text-red-400 bg-red-500/10` → `text-error-text bg-error/10`

**signUpForm.tsx:**
- Line 140: `text-red-400 bg-red-500/10` → `text-error-text bg-error/10`

**phoneVerification.tsx:**
- Line 148: `text-green-400 bg-green-500/10` → `text-success-text bg-success/10`

**emailVerification.tsx:**
- Line 117: `text-green-400 bg-green-500/10` → `text-success-text bg-success/10`
- Line 177: `text-green-400 bg-green-500/10` → `text-success-text bg-success/10`

Pattern: Replace all success messages with `text-success-text bg-success/10`, all error messages with `text-error-text bg-error/10`.

Do NOT change any functionality, only color classes.
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>All auth form error/success messages use semantic tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate locationSelector to semantic tokens</name>
  <files>src/components/features/locationSelector.tsx</files>
  <action>
Update locationSelector to use semantic tokens for error states:

- Line 82: `bg-red-500/10 border-red-500/20` → `bg-error/10 border-error/20`
- Line 86: `text-red-400` → `text-error-text`
- Line 88: `text-red-400` → `text-error-text`

These are all error state indicators for geolocation failures.

Do NOT change any functionality, only color classes.
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Location selector error states use semantic tokens</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] No hardcoded red-*/green-* colors remain in these 5 files
- [ ] Error messages display correctly (visual spot check)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 5 components migrated to semantic tokens
</success_criteria>

<output>
After completion, create `.planning/phases/14-module-polish/14-01-SUMMARY.md`
</output>
