---
phase: 14-module-polish
plan: 02
type: execute
---

<objective>
Migrate consumer deal components to semantic color tokens.

Purpose: Replace hardcoded Tailwind colors with semantic tokens in deal claiming and display components.
Output: 5 consumer-facing components using semantic tokens for status indicators.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-module-polish/14-01-SUMMARY.md

# Token migration pattern:
| Original | Semantic |
|----------|----------|
| bg-green-500/10 | bg-success/10 |
| text-green-400, text-green-500 | text-success-text |
| bg-red-500/10 | bg-error/10 |
| text-red-400 | text-error-text |

**Tech stack available:** Next.js 16, React, TypeScript, Tailwind CSS v4
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate claim flow components to semantic tokens</name>
  <files>src/components/features/claimDealModal.tsx, src/components/features/claimCard.tsx, src/components/features/claimCTA.tsx</files>
  <action>
Update claim components to use semantic color tokens:

**claimDealModal.tsx:**
- Lines 118-119: `bg-green-500/10 text-green-500` → `bg-success/10 text-success-text`
- Line 204: `text-red-400 bg-red-400/10` → `text-error-text bg-error/10`

**claimCard.tsx:**
- Lines 106-113: Multiple green-* classes for claimed status
  - `text-green-400` → `text-success-text`
  - `bg-green-500/10` → `bg-success/10`
  - `border-green-500/20` → `border-success/20`

**claimCTA.tsx:**
- Lines 61-62: `bg-green-500/10 text-green-500` → `bg-success/10 text-success-text`
- Lines 140-141: `bg-amber-400/10 text-amber-400` → `bg-warning/10 text-warning-text`

Do NOT change any functionality, only color classes.
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>All claim components use semantic tokens for status states</done>
</task>

<task type="auto">
  <name>Task 2: Migrate deal display components to semantic tokens</name>
  <files>src/components/features/dealCard.tsx, src/components/features/pricingBreakdown.tsx</files>
  <action>
Update deal display components:

**dealCard.tsx:**
- Line 109: `text-amber-400` (star rating) → KEEP as decorative color
  This is intentionally amber for star ratings, not a semantic status.

**pricingBreakdown.tsx:**
- Line 35: `text-green-400` → `text-success-text` (savings display)
- Lines 88-89: `text-green-400` → `text-success-text` (savings indicator)

Note: Star ratings use amber as decorative color - this is intentional per DESIGN-AUDIT.md.
Only migrate semantic colors (success for savings/positive values).
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Pricing and savings displays use semantic tokens</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] Claim status indicators use semantic tokens
- [ ] Savings/positive values use text-success-text
- [ ] Star ratings remain amber (intentional decorative color)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 5 consumer deal components migrated
</success_criteria>

<output>
After completion, create `.planning/phases/14-module-polish/14-02-SUMMARY.md`
</output>
