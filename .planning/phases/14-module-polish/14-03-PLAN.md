---
phase: 14-module-polish
plan: 03
type: execute
---

<objective>
Migrate business onboarding components to semantic color tokens.

Purpose: Replace hardcoded Tailwind colors with semantic tokens in business registration and profile forms.
Output: 4 business onboarding components using semantic tokens for validation states.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-module-polish/14-02-SUMMARY.md

# Token migration pattern:
| Original | Semantic |
|----------|----------|
| bg-green-500/10, bg-green-500/20 | bg-success/10, bg-success/20 |
| text-green-400, text-green-500 | text-success-text |
| border-red-500/50 | border-error/50 |
| text-red-400 | text-error-text |

**Tech stack available:** Next.js 16, React, TypeScript, Tailwind CSS v4
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate business form components to semantic tokens</name>
  <files>src/components/features/createBusinessForm.tsx, src/components/features/businessProfileForm.tsx</files>
  <action>
Update business form components to use semantic color tokens:

**createBusinessForm.tsx:**
- Line 245: `text-red-400` (required indicator) → `text-error-text`
- Line 263: `border-red-500/50 focus:border-red-500` → `border-error/50 focus:border-error`
- Line 276: `text-red-400` (error message) → `text-error-text`

**businessProfileForm.tsx:**
- Line 363: error styling → `text-error-text`
- Line 381: error styling → `border-error/*`
- Line 394: error styling → semantic tokens
- Line 478: `text-green-500` (verified status) → `text-success-text`

Pattern: All form validation errors use error tokens, verified/success states use success tokens.

Do NOT change any functionality, only color classes.
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Business form components use semantic tokens for validation</done>
</task>

<task type="auto">
  <name>Task 2: Migrate business flow components to semantic tokens</name>
  <files>src/components/features/claimBusinessFlow.tsx, src/components/features/businessInfo.tsx</files>
  <action>
Update business flow components:

**claimBusinessFlow.tsx:**
- Line 311: `text-red-400 bg-red-500/10` → `text-error-text bg-error/10`
- Lines 490-491: `bg-green-500/20 text-green-400` → `bg-success/20 text-success-text`

**businessInfo.tsx:**
- Lines 104-107: `bg-green-500/10 text-green-500` → `bg-success/10 text-success-text` (verified status)
- Lines 47, 56: `text-amber-400` (star rating) → KEEP as decorative color

Note: Star ratings are intentionally amber per DESIGN-AUDIT.md - do not migrate.
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Business flow components use semantic tokens</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] Form validation errors display in error color
- [ ] Verified/success states display in success color
- [ ] Star ratings remain amber (intentional decorative)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 4 business onboarding components migrated
</success_criteria>

<output>
After completion, create `.planning/phases/14-module-polish/14-03-SUMMARY.md`
</output>
