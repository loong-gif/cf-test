---
phase: 14-module-polish
plan: 04
type: execute
---

<objective>
Migrate monetization and payment components to semantic color tokens.

Purpose: Replace hardcoded Tailwind colors with semantic tokens in billing, payment, and pricing components.
Output: 6 monetization components using semantic tokens for status and pricing indicators.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-module-polish/14-03-SUMMARY.md

# Token migration pattern:
| Original | Semantic |
|----------|----------|
| bg-green-500/10 | bg-success/10 |
| text-green-400 | text-success-text |
| bg-red-500/10 | bg-error/10 |
| text-red-400 | text-error-text |
| bg-amber-500/10, bg-amber-500/5 | bg-warning/10, bg-warning/5 |
| text-amber-400 | text-warning-text |

**Tech stack available:** Next.js 16, React, TypeScript, Tailwind CSS v4
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate payment components to semantic tokens</name>
  <files>src/components/features/paymentMethods.tsx, src/components/features/billingHistory.tsx, src/components/features/creditPurchaseModal.tsx</files>
  <action>
Update payment components to use semantic color tokens:

**paymentMethods.tsx:**
- Line 29: `bg-red-500` (Mastercard logo) → KEEP (brand color)
- Line 30: `bg-amber-500` (Mastercard logo) → KEEP (brand color)
- Line 153: `hover:text-red-400` (delete hover) → `hover:text-error-text`

Note: Card brand colors (Visa blue, Mastercard red/amber) are intentional brand colors per DESIGN-AUDIT.md.

**billingHistory.tsx:**
- Lines 164-167: `bg-red-500/10 text-red-400` (payment failed) → `bg-error/10 text-error-text`

**creditPurchaseModal.tsx:**
- Line 85: success indicator → `text-success-text`
- Line 111: `bg-blue-600` (card icon) → KEEP (brand color)
- Lines 133, 151-152: success states → `bg-success/10 text-success-text`
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Payment components use semantic tokens for status states</done>
</task>

<task type="auto">
  <name>Task 2: Migrate pricing components to semantic tokens</name>
  <files>src/components/features/pricingTierCard.tsx, src/components/features/leadCreditsDisplay.tsx, src/components/features/leadPricingCard.tsx</files>
  <action>
Update pricing components:

**pricingTierCard.tsx:**
- Line 108: `text-green-400` (feature check) → `text-success-text`

**leadCreditsDisplay.tsx:**
- Lines 71-73: `bg-amber-500/5 text-amber-400` (low credits warning) → `bg-warning/5 text-warning-text`

**leadPricingCard.tsx:**
- Lines 69-92: Various green-* for positive indicators → `text-success-text`, `bg-success/10`

Pattern: Positive indicators use success tokens, warnings use warning tokens.
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Pricing components use semantic tokens</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] Payment failed states show error color
- [ ] Success/positive indicators show success color
- [ ] Low credits warning shows warning color
- [ ] Card brand logos remain unchanged (Visa blue, Mastercard red/amber)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 6 monetization components migrated
</success_criteria>

<output>
After completion, create `.planning/phases/14-module-polish/14-04-SUMMARY.md`
</output>
