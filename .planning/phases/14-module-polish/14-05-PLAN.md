---
phase: 14-module-polish
plan: 05
type: execute
---

<objective>
Migrate consumer and business dashboard pages to semantic color tokens.

Purpose: Replace hardcoded Tailwind colors with semantic tokens in dashboard page files.
Output: 7 page files using semantic tokens for status indicators and metrics.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-module-polish/14-04-SUMMARY.md

# Token migration pattern:
| Original | Semantic |
|----------|----------|
| bg-green-500/10 | bg-success/10 |
| text-green-400 | text-success-text |
| bg-red-500/10 | bg-error/10 |
| text-red-400 | text-error-text |
| bg-yellow-500/10 | bg-warning/10 |
| text-yellow-400 | text-warning-text |
| bg-blue-500/10 | bg-info/10 |
| text-blue-400 | text-info-text |

**Tech stack available:** Next.js 16, React, TypeScript, Tailwind CSS v4
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate consumer dashboard pages to semantic tokens</name>
  <files>src/app/dashboard/page.tsx, src/app/dashboard/settings/page.tsx</files>
  <action>
Update consumer dashboard pages:

**dashboard/page.tsx:**
- Line 21: `bg-green-500/10 text-green-400` (active status) → `bg-success/10 text-success-text`
- Lines 28, 35: `bg-yellow-500/10 text-yellow-400` (pending status) → `bg-warning/10 text-warning-text`
- Line 42: `bg-red-500/10 text-red-400` (expired status) → `bg-error/10 text-error-text`
- Lines 86-87: `bg-green-500/10 text-green-400` → `bg-success/10 text-success-text`
- Lines 101-102: `bg-blue-500/10 text-blue-400` (profile icon) → `bg-info/10 text-info-text`

**dashboard/settings/page.tsx:**
- Lines 59-60: `bg-green-500/10 text-green-400` (notification setting) → `bg-success/10 text-success-text`
- Lines 79-80: `bg-red-500/10 text-red-400` (sign out) → `bg-error/10 text-error-text`
- Line 118: `text-red-400` → `text-error-text`
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Consumer dashboard pages use semantic tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate business landing pages to semantic tokens</name>
  <files>src/app/business/page.tsx, src/app/business/create/page.tsx</files>
  <action>
Update business landing pages:

**business/page.tsx:**
- Line 103: `text-green-400` (positive indicator) → `text-success-text`

**business/create/page.tsx:**
- Line 241: `text-red-400 bg-red-500/10` (error message) → `text-error-text bg-error/10`
- Lines 309-310: `bg-green-500/20 text-green-400` (success state) → `bg-success/20 text-success-text`
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Business landing pages use semantic tokens</done>
</task>

<task type="auto">
  <name>Task 3: Migrate business dashboard pages to semantic tokens</name>
  <files>src/app/business/dashboard/page.tsx, src/app/business/dashboard/settings/account/page.tsx, src/app/business/dashboard/settings/account/checkout/page.tsx</files>
  <action>
Update business dashboard pages:

**business/dashboard/page.tsx:**
- Line 29: `text-green-400` (positive metric) → `text-success-text`
- Line 31: `text-red-400` (negative metric) → `text-error-text`

**business/dashboard/settings/account/page.tsx:**
- Lines 121-124: `bg-green-500/10 text-green-400` (success states) → `bg-success/10 text-success-text`
- Line 160: success indicator → `text-success-text`
- Lines 235-236: `bg-amber-500/10 text-amber-400` (warning) → `bg-warning/10 text-warning-text`
- Line 282: `text-red-400 hover:text-red-300 hover:bg-red-500/10` (cancel action) → `text-error-text hover:text-error-text/80 hover:bg-error/10`

**business/dashboard/settings/account/checkout/page.tsx:**
- Lines 58-59: success states → `bg-success/10 text-success-text`
- Line 109: success indicator → `text-success-text`
- Lines 144-145: success states → `bg-success/10 text-success-text`
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Business dashboard pages use semantic tokens</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migrated 7 dashboard page files to semantic color tokens</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Consumer Dashboard (http://localhost:3000/dashboard):
       - Verify: Active claims show green (success color)
       - Verify: Pending claims show yellow/amber (warning color)
       - Verify: Expired claims show red (error color)
    3. Consumer Settings (http://localhost:3000/dashboard/settings):
       - Verify: Notification toggles display correctly
       - Verify: Sign out button has appropriate styling
    4. Business Portal (http://localhost:3000/business):
       - Verify: Positive indicators show success color
    5. Business Dashboard (http://localhost:3000/business/dashboard):
       - Verify: Metrics display positive (green) and negative (red) correctly
    6. Business Settings (http://localhost:3000/business/dashboard/settings/account):
       - Verify: Success states show success color
       - Verify: Warning indicators show warning color
       - Verify: Cancel actions show error color
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] Status indicators use correct semantic colors
- [ ] Metrics display positive/negative values correctly
- [ ] Human verification approved
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Human verification approved
- 7 page files migrated
</success_criteria>

<output>
After completion, create `.planning/phases/14-module-polish/14-05-SUMMARY.md`
</output>
