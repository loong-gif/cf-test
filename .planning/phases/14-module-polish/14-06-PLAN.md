---
phase: 14-module-polish
plan: 06
type: execute
---

<objective>
Migrate business deal and lead management components to semantic color tokens.

Purpose: Replace hardcoded Tailwind colors with semantic tokens in deal management and lead components.
Output: 5 business management components using semantic tokens for actions and status.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-module-polish/14-05-SUMMARY.md

# Token migration pattern:
| Original | Semantic |
|----------|----------|
| bg-green-500/10 | bg-success/10 |
| text-green-400 | text-success-text |
| bg-red-500/10 | bg-error/10 |
| text-red-400 | text-error-text |
| bg-amber-500/10 | bg-warning/10 |
| text-amber-400 | text-warning-text |
| bg-blue-500/10 | bg-info/10 |
| text-blue-400, text-blue-500 | text-info-text |

**Tech stack available:** Next.js 16, React, TypeScript, Tailwind CSS v4
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate deal management components to semantic tokens</name>
  <files>src/components/features/dealManagement/dealList.tsx, src/components/features/dealManagement/dealForm.tsx</files>
  <action>
Update deal management components:

**dealList.tsx:**
- Line 372: `text-red-400 hover:text-red-300` (delete action) → `text-error-text hover:text-error-text/80`

**dealForm.tsx:**
- Lines 302, 308: error state styling → `text-error-text`, `border-error/*`
- Lines 477, 483: error state styling → semantic tokens
- Lines 539-540: `bg-green-500/10 text-green-400` (success message) → `bg-success/10 text-success-text`
- Lines 547, 548: error styling → `text-error-text`

Pattern: Delete/destructive actions use error tokens, success messages use success tokens.
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Deal management components use semantic tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate lead management and sponsored components to semantic tokens</name>
  <files>src/components/features/leadManagement/leadList.tsx, src/components/features/leadManagement/leadDetail.tsx, src/components/features/sponsoredDealConfig.tsx</files>
  <action>
Update lead management and sponsored deal components:

**leadList.tsx:**
- Line 136: `text-amber-400` (low credits) → `text-warning-text`

**leadDetail.tsx:**
- Lines 291-292: `bg-green-500/10 text-green-400` (contact icons) → `bg-success/10 text-success-text`
- Lines 305-306: `bg-blue-500/10 text-blue-500` (phone icon) → `bg-info/10 text-info-text`

**sponsoredDealConfig.tsx:**
- Line 276: `text-green-400` (trend indicator) → `text-success-text`

Also check business/dashboard/deals/sponsored/page.tsx:
- Line 417: `text-green-400` (trend indicator) → `text-success-text`
- Line 151: `text-amber-400` (featured icon) → KEEP as decorative
  </action>
  <verify>`npm run build` passes, no TypeScript errors</verify>
  <done>Lead and sponsored components use semantic tokens</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] Delete actions show error color
- [ ] Success messages show success color
- [ ] Low credits warning shows warning color
- [ ] Contact icons use appropriate semantic colors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 5+ components migrated (includes sponsored page)
- Phase 14 complete
</success_criteria>

<output>
After completion, create `.planning/phases/14-module-polish/14-06-SUMMARY.md`

Include Phase 14 completion summary:
- Total files migrated
- Semantic tokens now consistently used
- Ready for Phase 15: Admin Polish & QA
</output>
