---
phase: 24-seo-foundation
plan: 03
type: execute
---

<objective>
Create reusable JSON-LD React components for structured data injection across the application.

Purpose: Provide drop-in components that emit Schema.org structured data to enhance search result appearance (rich snippets, knowledge panels).
Output: src/components/seo/ directory with WebsiteSchema, OrganizationSchema, BreadcrumbSchema components integrated into root layout
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-seo-foundation/24-RESEARCH.md
@.planning/phases/24-seo-foundation/24-01-SUMMARY.md
@.planning/phases/24-seo-foundation/24-02-SUMMARY.md

**From 24-02:** SEO utilities in src/lib/seo/ with schema builder functions

**Component patterns from research:**
- JSON-LD injected via <script type="application/ld+json">
- Use dangerouslySetInnerHTML with JSON.stringify
- Components are Server Components (no 'use client')
- next-seo provides ready-made components but we can also use our builders

**Root layout integration:**
- Add base schemas (WebSite, Organization) to root layout
- BreadcrumbSchema used per-page in location pages (Phase 25+)

@src/lib/seo/schemas.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SEO component directory with schema components</name>
  <files>src/components/seo/websiteSchema.tsx, src/components/seo/organizationSchema.tsx, src/components/seo/breadcrumbSchema.tsx, src/components/seo/index.ts</files>
  <action>
    Create src/components/seo/ directory with four files:

    **src/components/seo/websiteSchema.tsx:**
    - Import buildWebsiteSchema from '@/lib/seo'
    - Export function WebsiteSchema() as Server Component
    - Call buildWebsiteSchema() to get JSON-LD object
    - Return <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} />

    **src/components/seo/organizationSchema.tsx:**
    - Same pattern with buildOrganizationSchema()
    - Include CostFinders organization data (name, url, logo, social profiles)

    **src/components/seo/breadcrumbSchema.tsx:**
    - Accept props: items: Array<{ name: string; url: string }>
    - Import buildBreadcrumbSchema from '@/lib/seo'
    - Return script tag with breadcrumb JSON-LD
    - This component will be used in location pages (Phase 25+)

    **src/components/seo/index.ts:**
    - Barrel export all three components

    All components are Server Components (no 'use client' directive).
  </action>
  <verify>Run `npm run build` to verify components compile. No TypeScript errors.</verify>
  <done>Four files created in src/components/seo/, all type-check successfully</done>
</task>

<task type="auto">
  <name>Task 2: Integrate base schemas into root layout</name>
  <files>src/app/layout.tsx</files>
  <action>
    Update src/app/layout.tsx:
    1. Import WebsiteSchema, OrganizationSchema from '@/components/seo'
    2. Add both components inside <body> but BEFORE the provider tree:
       <WebsiteSchema />
       <OrganizationSchema />
    3. Place them immediately after opening <body> tag, before LocationProvider

    This ensures every page has base structured data about the site/organization.
    BreadcrumbSchema is NOT added here — it's page-specific.
  </action>
  <verify>Run `npm run dev`, view page source, confirm JSON-LD script tags present in HTML</verify>
  <done>Root layout includes WebsiteSchema and OrganizationSchema components, visible in page source</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete SEO foundation with structured data in page source</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000
    3. View page source (Cmd+Option+U or right-click → View Page Source)
    4. Search for "application/ld+json" in the source
    5. Confirm: Two JSON-LD script blocks visible (WebSite and Organization types)
    6. Visit: http://localhost:3000/robots.txt
    7. Confirm: Valid robots.txt with sitemap reference
    8. Visit: http://localhost:3000/sitemap.xml
    9. Confirm: Valid XML sitemap with page URLs
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] JSON-LD visible in page source
- [ ] robots.txt accessible
- [ ] sitemap.xml accessible
- [ ] No TypeScript errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Structured data visible in page source
- Phase 24 SEO Foundation complete
</success_criteria>

<output>
After completion, create `.planning/phases/24-seo-foundation/24-03-SUMMARY.md`:

# Phase 24 Plan 03: Structured Data Components Summary

**[One-liner summary]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- [File list]

## Decisions Made

[Key decisions]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Phase 24 complete, ready for Phase 25: State Pages
</output>
