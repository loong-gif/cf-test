---
phase: 29-service-category-pages
plan: 01
type: execute
---

<objective>
Create SEO-optimized service category landing pages (Botox, Fillers, Facials, Laser, Body, Skincare) with proper routing, metadata generation, and sitemap integration.
</objective>

<context>
Following Phase 28's provider pages pattern. Service category pages will live at `/treatments/[category]` and display all deals for a specific treatment category across all locations. This complements the location-based hierarchy with treatment-based navigation.
</context>

<tasks>

## Task 1: Add category mock data helpers

**File:** `src/lib/mock-data/categories.ts`

Add helper functions:
- `getAllCategorySlugs()` - returns all category slugs for static params
- `getCategoryWithStats(slug)` - returns category with dealCount, businessCount
- `getDealsForCategory(categoryId)` - returns all deals for a category

**Acceptance:** Functions exported and return correct data shapes

---

## Task 2: Create category page route

**File:** `src/app/treatments/[category]/page.tsx`

Create SSG-optimized category landing page:
- `generateStaticParams()` - generate routes for all categories
- `generateMetadata()` - SEO metadata with category name and deal count
- Page component with:
  - BreadcrumbSchema for structured data
  - Hero section with category icon, name, description
  - Stats row (deal count, provider count)
  - Deals grid using DealCard component
  - Empty state for categories with no deals

**Pattern:** Follow neighborhood page structure

**Acceptance:** Page renders at `/treatments/botox`, `/treatments/fillers`, etc.

---

## Task 3: Add SEO utilities and update sitemap

**Files:**
- `src/lib/seo/metadata.ts` - add `generateCategoryMetadata()`
- `src/app/sitemap.ts` - add category pages to sitemap

Add category-specific metadata generation and include all category URLs in sitemap with priority 0.7.

**Acceptance:** Category pages appear in sitemap, metadata generates correctly

</tasks>

<validation>
- [ ] All 6 category pages render without errors
- [ ] Breadcrumb navigation works correctly
- [ ] Deals display for each category
- [ ] Sitemap includes category URLs
- [ ] No TypeScript errors
</validation>
